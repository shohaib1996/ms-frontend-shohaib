apiVersion: v1
kind: Service
metadata:
  name: bh-frontend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: bh-frontend
  type: NodePort
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
